package com.nakyoung.androidclientdevelopment.statics

import com.nakyoung.androidclientdevelopment.R

enum class MENU(val id:Int) {
    TODAY(R.id.today),
    TIME_LINE(R.id.timeline),
    PROFILE(R.id.profile)
}